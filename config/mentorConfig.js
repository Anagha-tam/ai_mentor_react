/**
 * Configuration for AI Mentor LiveKit integration (sandbox-based).
 *
 * Authentication uses LiveKit's sandbox token server:
 * TokenSource.sandboxTokenServer(SANDBOX_ID).
 */
export const MENTOR_CONFIG = {
  SANDBOX_ID: '',
  AGENT_NAME: '',
  ROOM_NAME: 'mentor-room',
};
